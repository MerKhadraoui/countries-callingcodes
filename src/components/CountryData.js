import React from "react";
const CountryData = ({ result }) => {
  console.log({ result });
  //const country=result.map(())
  return (
    <React.Fragment>
      name alpha2Code callingCodes capital languages.nativeName flag
    </React.Fragment>
  );
};
export default CountryData;
